import { api } from "@/lib/api"
import { Category } from "@/types"
import { useQuery } from "@tanstack/react-query"

interface QuizzesByCategoryProps {
  slug: string
}

export function QuizzesByCategory({slug}: QuizzesByCategoryProps){

  return (

  )
}